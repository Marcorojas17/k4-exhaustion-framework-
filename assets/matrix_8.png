from PIL import Image
import os
os.makedirs("assets", exist_ok=True)

taps = [1 if i in [0, 1, 3, 4, 23] else 0 for i in range(24)]
m8 = Image.new("1", (24,1))
m8.putdata(taps) # directo 0/1, no 255
m8.save("assets/matrix_8.png")
print("[+] matrix_8.png - taps:", [0,1,3,4,23])
