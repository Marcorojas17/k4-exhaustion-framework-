from PIL import Image
import numpy as np
import os
os.makedirs("assets", exist_ok=True)

data = np.array([i % 26 for i in range(344)], dtype=np.uint8).reshape(4,86) * 9
Image.fromarray(data, 'L').save("assets/matrix_4.png")
print("[+] matrix_4.png")
